version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - echo ">>> Running buildspec.yml"
      - npm run lint
      - npm test

artifacts:
  files:
    - '**/*'



